[package]
name = "rust"
version = "0.1.0"
edition = "2024"

[dependencies]

rand = "0.9"

ndarray = "0.15"

candle-core = "0.8.2"
candle-nn = "0.8.2"

serde = {version="1.0",features=["derive"]}

serde_json = "1.0"
